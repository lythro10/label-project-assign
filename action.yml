name: 'The name of your action here'
description: 'Provide a description here'
author: 'George Michael'

# Define your inputs here.
inputs:
  GITHUB_TOKEN:
    description: 'GitHub token'
    required: true
  selected_system:
    description: 'For which dropdown option would you like to run'
    required: true
  selected_system_label:
    description: 'Label you would like to add'
    required: true



outputs:
runs:
  using: node16
  main: dist/index.js
